{{ logfile }}
{
        rotate 7
        daily
        missingok
        notifempty
        delaycompress
        compress
        postrotate
          sudo systemctl reload rsyslog >/dev/null 2>&1 || true
        endscript
}
